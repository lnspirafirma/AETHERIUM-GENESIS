# ...
services:
  # 1. Main Service (รวม Config เดิม + ตัวแปรใหม่)
  aether-core:
    # ... (ส่วนบนเหมือนเดิม)
    environment:
      - PYTHONUNBUFFERED=1
      - OTEL_PYTHON_LOG_CORRELATION=true  # <-- O11y Enabled
    # ❌ แก้ไขจาก: command: python main_simulation.py
    # ✅ เป็น:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    restart: unless-stopped
    # ...