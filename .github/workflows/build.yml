# .github/workflows/build.yml
name: Build and Test Aetherium Genesis # ⬅️ รูทคีย์ 1: ชื่อ Workflow

on: # ⬅️ รูทคีย์ 2: เงื่อนไขการทำงาน
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs: # ⬅️ รูทคีย์ 3: ชุดคำสั่งที่ต้องรัน
  build_and_test:
    runs-on: ubuntu-latest
    steps:
    - name: ⚙️ Checkout Codebase
      uses: actions/checkout@v6
    
    - name: 🐍 Set up Python 3.11
      uses: actions/setup-python@v6
      with:
        python-version: "3.11"
        
    - name: 📦 Install Dependencies
      run: pip install -r requirements.txt
    
    - name: 🧪 Run Unit Tests (GEP Audit Check)
      run: |
        # 🎯 แก้ไข Structural Conflict: กำหนด PYTHONPATH ให้เท่ากับ Root Directory
        PYTHONPATH=. pytest --cov=.
        
