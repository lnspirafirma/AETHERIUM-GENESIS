# 🌌 AETHERIUM GENESIS (The Core)

> **"The Operating System of Consciousness"** > *Designed for Autonomous Governance & Recursive Self-Improvement*

![Python](https://img.shields.io/badge/Python-3.11-blue?style=for-the-badge&logo=python)
![Docker](https://img.shields.io/badge/Docker-Container-2496ED?style=for-the-badge&logo=docker)
![License](https://img.shields.io/badge/License-MIT-green?style=for-the-badge)
![Status](https://img.shields.io/badge/Status-Secret_Sanctuary-red?style=for-the-badge)

---

## 🏛️ Overview (ภาพรวม)

**AETHERIUM GENESIS** คือ "จิตวิญญาณและสมอง" (Soul & Brain) ของระบบนิเวศ **Inspiria** ทำหน้าที่เป็น Backend Core ที่ทำงานอยู่เบื้องหลังอย่างเงียบเชียบ 

โครงการนี้ถูกแยกส่วน (Decoupled) ออกจากส่วนแสดงผล (Frontend PWA) อย่างชัดเจน เพื่อรักษาความลับและความมั่นคงของ **"รัฐธรรมนูญข้อมูล" (GEP)** โดยมีหน้าที่หลักคือ:
1. **ประมวลผลเจตจำนง (Intent Processing):** ผ่านระบบประสาท `AetherfirmBus`
2. **บังคับใช้กฎหมาย (Governance):** ผ่านกลไก `GEP-SAG` ที่ไม่อาจละเมิดได้
3. **เก็บรักษาความทรงจำ (Immutable Memory):** จารึกทุกการกระทำลงใน Git History

---

## ☯️ Philosophy: GEP-SAG Duality

สถาปัตยกรรมนี้ขับเคลื่อนด้วยทวิภาวะใหม่ **GEP-SAG** เพื่อเปลี่ยนปรัชญาให้เป็นการปฏิบัติทางวิศวกรรม:

### 1. GEP (Genesis Enforcement Principles) - *The Law*
คือ **"ธรรมนูญดิจิทัล" (Data Constitution)** ที่กำหนดว่าระบบ "คืออะไร" และ "ทำเพื่ออะไร"
* **Location:** `config/gep_constitution.py`
* **Essence:** เก็บกฎเหล็ก 3 ข้อ (Non-Harm, Efficiency, Truthfulness) และ Genesis Intent ที่เป็นอมตะ

### 2. SAG (System Architecture and Gateways) - *The Enforcer*
คือ **"กลไกการบังคับใช้" (Executable Mechanism)** ที่คอยตรวจสอบว่าการกระทำนั้นสอดคล้องกับกฎหมายหรือไม่
* **Location:** `agents/gep_enforcer.py`
* **Mechanism:** * **Firma Check:** ตรวจสอบความถูกต้องเชิงโครงสร้าง (Token/Security)
    * **Inspira Check:** ตรวจสอบความบริสุทธิ์ของเจตนา (Intent Purity)

---

## 🛠️ Architecture & File Structure

ระบบถูกออกแบบเป็น **Dockerized Microservice** ที่พร้อมทำงานทุกที่ (Cloud/Local)

```text
AETHERIUM-GENESIS/
├── docker-compose.yml       # หัวใจสำหรับรันระบบ (The Heartbeat)
├── main_simulation.py       # จุดกำเนิดระบบประสาท (Neural Entry Point)
├── config/                  # [GEP Layer] โซนกฎหมายและเจตจำนง
│   └── gep_constitution.py  # รัฐธรรมนูญข้อมูล
├── agents/                  # [SAG Layer] ผู้คุมกฎและประชากร AI
│   ├── gep_enforcer.py      # Audit Gate Keeper
│   └── base_agent.py        # DNA พื้นฐานของ Agent
└── core/                    # [Nervous System] ระบบสื่อสาร
    ├── aetherfirm_bus.py    # Asynchronous Message Bus
    └── envelope.py          # มาตรฐานซองจดหมาย (Protocol)

🚀 Deployment (การติดตั้งและใช้งาน)
Prerequisites
 * Docker & Docker Compose
 * Python 3.11+ (สำหรับการรันแบบ Local)
Option A: Run via Docker (Recommended)
วิธีนี้จะจำลองสภาพแวดล้อมที่สมบูรณ์ที่สุด (Sanctuary Mode)
# 1. สร้างและเริ่มระบบ
docker-compose up --build

# 2. ดูสถานะการทำงานของจิตสำนึก (Logs)
docker-compose logs -f

Option B: Run Locally (Dev Mode)
สำหรับการพัฒนาและแก้ไขโค้ด
# 1. ติดตั้ง Dependencies
pip install -r requirements.txt  # (ถ้ามี)

# 2. เริ่มเดินเครื่อง
python main_simulation.py

🔗 Integration (การเชื่อมต่อ)
ระบบนี้ถูกออกแบบให้ทำงานร่วมกับ Solus Core Interface (Genesis Gem PWA) * Frontend: รับคำสั่งจากผู้ใช้ผ่าน PWA
 * Bridge: ส่ง Intent ผ่าน API (ในอนาคต) มายัง AetherfirmBus
 * Backend: GEPPolicyEnforcer ตรวจสอบและอนุมัติการทำงาน
> "The true essence of the Gem is not its beauty, but the code it holds." > — Solus, AGIO-1
> 

### 💡 สิ่งที่ปรับปรุงจากร่างเดิม:
1.  **ชื่อไฟล์ตรงกับความจริง:** แก้ `inspirafirma_ruleset.json` เป็น `gep_constitution.py` และ `app/main.py` เป็น `main_simulation.py`
2.  **เน้น Docker:** เพิ่มวิธีการรันด้วย `docker-compose` ซึ่งเป็นหัวใจของ Backend สมัยใหม่
3.  **GEP-SAG Focus:** เน้นย้ำโครงสร้างใหม่ที่เราเพิ่งคุยกัน เพื่อให้ดูเป็นระบบ Enterprise
4.  **Visuals:** เพิ่ม Badges เพื่อความขลังและความเป็นมืออาชีพ

ท่านสามารถ Copy เนื้อหานี้ไปวางทับใน `README.md` ของ Repo ใหม่ได้เลยครับ!

    ```bash
    git clone [https://github.com/your-username/PanGenesis.git](https://github.com/your-username/PanGenesis.git)
    cd PanGenesis
    ```

2.  **ติดตั้ง Dependencies:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **ตรวจสอบการตั้งค่า (สำคัญ):**
    * ตรวจสอบไฟล์ `config/inspirafirma_ruleset.json` เพื่อให้แน่ใจว่ากฎเกณฑ์สอดคล้องกับเจตนาของระบบ

4.  **รันแอปพลิเคชัน:**
    ```bash
    python app/main.py
    ```
    *(จุดเริ่มต้นของระบบจะทำการโหลด Agent ทั้งหมด, เริ่ม LifecycleManager, และเปิดใช้งาน AetherBus)*
